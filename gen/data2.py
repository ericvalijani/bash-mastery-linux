# Days 11-20. tier: lint | ci | lab

DAYS_11_20 = [
dict(n=11, title="firewalld, and the nftables underneath it", runs="VM: node1", tier="lab",
  obj="Close everything, open only what you need, and see the kernel rules your commands produced.",
  why="Most people learn the front end and never look underneath, so they cannot debug it when the front end lies. Look at both.",
  work=["firewall-cmd --list-all, --zone", "--add-port / --add-service, --permanent, --reload", "rich rules", "nft list ruleset", "nft chains, hooks and priorities", "ss -tulpn to confirm exposure"],
  checks=[("auto","firewalld is running and enabled","systemctl is-active firewalld && systemctl is-enabled firewalld"),
    ("auto","your service port is open",'firewall-cmd --list-ports | grep -qE "(8080|443)/tcp"'),
    ("auto","the rule is permanent, not runtime only",'firewall-cmd --permanent --list-ports | grep -qE "(8080|443)/tcp"'),
    ("auto","firewalld built a real nftables table",'nft list ruleset | grep -q "table inet firewalld"'),
    ("auto","the default zone is not trusted",'[ "$(firewall-cmd --get-default-zone)" != "trusted" ]'),
    ("manual","you reloaded and the rules survived, and you can point to each chain","")]),

dict(n=12, title="SSH hardening, bastions and fail2ban", runs="VM: control + node1", tier="lab",
  obj="Reach node1 only through control, with keys only, and ban brute force.",
  why="SSH is the door. It is also the service most often left with defaults that a scanner finds in minutes.",
  work=["sshd -T to read the effective config", "/etc/ssh/sshd_config.d/", "PasswordAuthentication, PermitRootLogin", "AllowUsers, AllowGroups", "ProxyJump and ~/.ssh/config", "fail2ban-client status sshd"],
  checks=[("auto","password authentication is off",'sshd -T | grep -qx "passwordauthentication no"'),
    ("auto","root cannot log in with a password",'sshd -T | grep -qE "^permitrootlogin (no|prohibit-password)$"'),
    ("auto","login is restricted to a named user or group",'sshd -T | grep -qE "^(allowusers|allowgroups) "'),
    ("auto","the config is valid","sshd -t"),
    ("auto","fail2ban is watching sshd","fail2ban-client status sshd"),
    ("manual","you reached node1 with ProxyJump through control, and direct access is refused",""),
    ("manual","you triggered a ban on purpose and then unbanned yourself","")]),

dict(n=13, title="SELinux: contexts, booleans and denial triage", runs="VM: node1", tier="lab",
  obj="Serve content from a non-default path with SELinux still enforcing.",
  why="This is the day that separates operators from people who type setenforce 0. Denial triage is a procedure, and once you know it, SELinux stops being an obstacle.",
  work=["getenforce, sestatus, setenforce", "ls -Z, ps -Z, id -Z", "semanage fcontext -a -t, restorecon", "chcon vs semanage (temporary vs permanent)", "getsebool -a, setsebool -P", "ausearch -m avc -ts recent, audit2allow, sealert"],
  checks=[("auto","SELinux is enforcing",'[ "$(getenforce)" = "Enforcing" ]'),
    ("auto","the web root carries a web content label",'ls -Zd /srv/www | grep -q "httpd_sys_content_t"'),
    ("auto","the label rule is permanent, not just a chcon",'semanage fcontext -l | grep -q "/srv/www"'),
    ("auto","restorecon is a no-op, so labels match policy",'[ -z "$(restorecon -nvR /srv/www)" ]'),
    ("auto","nginx actually serves the content","curl -sf http://localhost/ >/dev/null"),
    ("auto","a boolean you set survives, recorded as permanent",'semanage boolean -l -C | grep -q .'),
    ("auto","a custom policy module is loaded",'semodule -l | grep -qE "lab"'),
    ("manual","you fixed a denial by relabelling, not by disabling SELinux",""),
    ("manual","you built that module from a real AVC with audit2allow and read it before loading","")]),

dict(n=14, title="Ansible fundamentals", runs="control -> node1", tier="lab",
  obj="Replace one day of manual work with a playbook that is safe to run twice.",
  why="You have now configured hosts by hand for thirteen days. This is the day that work becomes repeatable, and idempotency stops being a buzzword.",
  work=["inventory: INI and YAML", "ansible -m ping, ansible-inventory --list", "ansible-playbook --check --diff", "modules: package, service, copy, template, user, lineinfile", "handlers and notify", "variables, group_vars, host_vars"],
  checks=[("auto","the inventory parses","ansible-inventory --list >/dev/null"),
    ("auto","node1 answers a ping module","ansible node1 -m ping"),
    ("auto","the playbook has valid syntax","ansible-playbook site.yml --syntax-check"),
    ("auto","a first run completes with no failures",'ansible-playbook site.yml | grep -q "failed=0"'),
    ("auto","a second run changes nothing",'ansible-playbook site.yml | grep -q "changed=0"'),
    ("manual","--check predicted the same changes the real run made",""),
    ("manual","you can explain which module was not idempotent and why","")]),

dict(n=15, title="Ansible roles: your hardening baseline", runs="control -> node1 + node2", tier="lab",
  obj="Turn Days 11-13 into one role and apply it to a host that has never been touched.",
  why="A baseline you can apply to a fresh machine in one command is the whole point of configuration management. node2 is the proof, because it starts clean.",
  work=["ansible-galaxy init roles/hardening", "tasks, handlers, defaults, templates, files", "role dependencies and tags", "ansible-vault for secrets", "--limit and --tags", "drift detection with --check"],
  checks=[("auto","the role has the standard layout","[ -d roles/hardening/tasks ] && [ -f roles/hardening/tasks/main.yml ]"),
    ("auto","it applies to both nodes without failures",'ansible-playbook site.yml -l node1,node2 | grep -q "failed=0"'),
    ("auto","a second run is a no-op on both",'[ "$(ansible-playbook site.yml -l node1,node2 | grep -c "changed=0")" -ge 2 ]'),
    ("auto","node2 ends up enforcing SELinux too",'ansible node2 -a "getenforce" | grep -q "Enforcing"'),
    ("auto","node2 ends up with password auth disabled",'ansible node2 -a "sshd -T" | grep -qx "passwordauthentication no"'),
    ("manual","you broke a setting on node1 by hand and --check found the drift",""),
    ("manual","secrets live in vault, not in the repo","")]),

dict(n=16, title="WireGuard: a private network between hosts", runs="VM: control + node1", tier="lab",
  obj="Build an encrypted tunnel between two machines and make it come back after reboot.",
  why="Every real environment has a management network you are not supposed to expose. WireGuard is small enough to understand completely, which is rare in VPNs.",
  work=["wg genkey / pubkey", "/etc/wireguard/wg0.conf", "AllowedIPs as a routing table", "wg-quick up / down, wg show", "systemctl enable wg-quick@wg0", "firewalld and the tunnel port"],
  checks=[("auto","the wg0 interface exists","wg show wg0"),
    ("auto","a peer is configured",'wg show wg0 peers | grep -q .'),
    ("auto","a handshake has happened",'[ "$(wg show wg0 latest-handshakes | awk "{print \\$2}")" != "0" ]'),
    ("auto","the tunnel address is reachable","ping -c1 -W2 10.20.0.2 || ping -c1 -W2 10.20.0.1"),
    ("auto","the tunnel comes up at boot","systemctl is-enabled wg-quick@wg0"),
    ("auto","the private key is not world readable",'[ "$(stat -c %a /etc/wireguard/wg0.conf)" -le 600 ]'),
    ("manual","you can explain what AllowedIPs does in both directions","")]),

dict(n=17, title="Reverse proxy and TLS termination", runs="VM: node1", tier="lab",
  obj="Put nginx in front of a plain HTTP service and serve it over TLS with your own CA.",
  why="This is how almost every internal service is actually exposed. It also ties Day 10 to something running, and Day 13 will fight you here.",
  work=["nginx server blocks, proxy_pass", "ssl_certificate, ssl_protocols, ssl_ciphers", "HTTP to HTTPS redirect", "X-Forwarded-For and proxy headers", "nginx -t, nginx -T", "SELinux: httpd_can_network_connect"],
  checks=[("auto","the nginx config is valid","nginx -t"),
    ("auto","nginx is running and enabled","systemctl is-active nginx && systemctl is-enabled nginx"),
    ("auto","HTTPS works and verifies against your CA","curl -sf --cacert /etc/pki/ca-trust/source/anchors/lab-ca.crt https://www.lab.test/ >/dev/null"),
    ("auto","plain HTTP redirects instead of serving",'[ "$(curl -s -o /dev/null -w %{http_code} http://www.lab.test/)" -ge 301 ]'),
    ("auto","obsolete TLS versions are refused","! openssl s_client -connect www.lab.test:443 -tls1_1 </dev/null 2>/dev/null | grep -q BEGIN"),
    ("auto","the backend is not reachable from outside","! curl -sf --max-time 3 http://www.lab.test:8080/ >/dev/null"),
    ("manual","you hit an SELinux denial on proxy_pass and fixed it with a boolean","")]),

dict(n=18, title="Bridges, VLANs and link aggregation", runs="Host: network namespaces", tier="ci",
  obj="Segment one wire into isolated networks, then prove the isolation.",
  why="Every hypervisor and every switch does this. It is also how you explain to yourself why two machines on the same cable cannot see each other.",
  work=["ip link add type bridge, brctl show", "ip link add link eth0 type vlan id 10", "bridge vlan show, tagged vs untagged", "ip link add type bond", "forwarding and STP basics", "testing isolation with ping"],
  checks=[("auto","a bridge exists in the router namespace",'ip netns exec router ip -d link show type bridge | grep -q "br0"'),
    ("auto","a VLAN interface with a tag exists",'ip netns exec router ip -d link show | grep -q "vlan id 10"'),
    ("auto","two hosts in the same VLAN can reach each other","ip netns exec client ping -c1 -W2 10.30.10.3"),
    ("auto","a host in a different VLAN cannot","! ip netns exec client ping -c1 -W2 10.30.20.3"),
    ("auto","the bridge has interfaces enslaved to it",'ip netns exec router ip link show master br0 | grep -q .'),
    ("manual","you can explain tagged versus untagged from the bridge vlan output","")]),

dict(n=19, title="Intrusion detection and audit alerting", runs="VM: node1", tier="lab",
  obj="Notice an attack and a file change without watching the screen.",
  why="Hardening stops the easy attempts. Detection is what tells you about the rest, and auditd is the tool that answers who changed this file.",
  work=["suricata -T, /etc/suricata/suricata.yaml", "eve.json and jq", "custom rules and sid numbering", "auditctl -w, -k, /etc/audit/rules.d/", "ausearch -k, aureport", "journald and forwarding"],
  checks=[("auto","suricata config passes its own test","suricata -T -c /etc/suricata/suricata.yaml >/dev/null"),
    ("auto","suricata is running and enabled","systemctl is-active suricata && systemctl is-enabled suricata"),
    ("auto","at least one alert has been written",'grep -q "alert" /var/log/suricata/eve.json'),
    ("auto","auditd watches a sensitive file",'auditctl -l | grep -q "/etc/shadow"'),
    ("auto","the audit rule is persistent",'grep -rq "/etc/shadow" /etc/audit/rules.d/'),
    ("auto","an audit event was actually recorded",'ausearch -k shadow_watch 2>/dev/null | grep -q "type=SYSCALL"'),
    ("manual","you triggered your own rule on purpose and found the alert",""),
    ("manual","you tuned out one false positive and can justify it","")]),

dict(n=20, title="Backup, restore and the restore drill", runs="VM: control + node1", tier="lab",
  obj="Prove you can get the data back, on a schedule, and know how long it takes.",
  why="An untested backup is a rumour. This is the last day because it is the one that decides whether everything before it was worth configuring.",
  work=["restic init / backup / snapshots", "restic restore --target, restic check", "retention: forget --keep-daily --prune", "systemd timers vs cron", "repository password handling", "diff -r to verify a restore"],
  checks=[("auto","the repository exists and is readable","restic snapshots >/dev/null"),
    ("auto","at least one snapshot exists",'[ "$(restic snapshots --json | grep -c short_id)" -ge 1 ]'),
    ("auto","the repository passes an integrity check","restic check"),
    ("auto","a restore reproduces the source tree exactly","diff -r /srv/data /var/tmp/restore/srv/data"),
    ("auto","backups run on a timer","systemctl is-enabled restic-backup.timer"),
    ("auto","the timer has actually fired at least once",'systemctl show restic-backup.timer -p LastTriggerUSec | grep -qv "=0$"'),
    ("auto","a retention policy is configured",'grep -rq "keep-daily" /etc/systemd/system/ /usr/local/bin/ 2>/dev/null'),
    ("manual","you timed a full restore and can state the number in minutes",""),
    ("manual","the repository password is not stored next to the repository","")]),
]
